<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ChoiceBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.PasswordField?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<StackPane xmlns:fx="http://javafx.com/fxml"
           fx:controller="com.application.strms.presentation.controller.pages.AddUserController"
           styleClass="page-root">

    <VBox alignment="CENTER" spacing="30.0" styleClass="card add-user-card">
        <padding>
            <Insets bottom="50.0" left="40.0" right="40.0" top="50.0"/>
        </padding>

        <VBox alignment="CENTER" spacing="8">
            <Label text="STRMS" styleClass="page-title"/>
            <Label text="Create a new user." styleClass="page-subtitle"/>
        </VBox>

        <VBox spacing="15">
            <TextField fx:id="nameField" promptText="Name" styleClass="input-field"/>
            <TextField fx:id="emailField" promptText="Email" styleClass="input-field"/>
            <PasswordField fx:id="passwordField" promptText="Password" styleClass="input-field"/>
            <ChoiceBox fx:id="roleChoiceBox" maxWidth="Infinity" styleClass="role-choice"/>
        </VBox>

        <Label fx:id="errorLabel"
               text="Unable to create user"
               styleClass="error-label"
               managed="false"
               visible="false"/>

        <VBox spacing="12">
            <Button fx:id="addUserButton"
                    text="Create user"
                    onAction="#addUser"
                    maxWidth="Infinity"
                    styleClass="primary-button"/>

            <Button text="Back"
                    onAction="#goBack"
                    maxWidth="Infinity"
                    styleClass="secondary-button"/>
        </VBox>
    </VBox>
</StackPane>